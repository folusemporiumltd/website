'use client'
import {useState} from 'react'
export default function SocialShare({title,path}:{title:string;path:string}){const [copied,setCopied]=useState(false);const base=typeof window!=='undefined'?window.location.origin:'https://website-smoky-kappa-22.vercel.app',url=`${base}${path}`,text=`Check out ${title} from Folus Emporium`;async function copy(){await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <div style={{marginTop:24}}><strong style={{display:'block',marginBottom:10}}>Share this product</strong><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><a className="btn btn-outline" target="_blank" rel="noopener noreferrer" href={`https://wa.me/?text=${encodeURIComponent(`${text} ${url}`)}`}>WhatsApp</a><a className="btn btn-outline" target="_blank" rel="noopener noreferrer" href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`}>Facebook</a><a className="btn btn-outline" target="_blank" rel="noopener noreferrer" href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(text)}&url=${encodeURIComponent(url)}`}>X</a><button className="btn btn-outline" type="button" onClick={copy}>{copied?'Copied!':'Copy link'}</button></div></div>}
