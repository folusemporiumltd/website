import Link from 'next/link'
import { notFound } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
export default async function CmsPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await createClient();const {data:p}=await s.from('content_pages').select('*').eq('slug',slug).eq('is_published',true).maybeSingle();if(!p)notFound();return <main><header className="nav"><div className="container nav-inner"><Link className="brand" href="/"><img src="/folus-emporium-circular-logo.png" alt="Folus Emporium logo"/><span>FOLUS<br/>EMPORIUM<small>Curating Excellence for Life’s Finest Moments</small></span></Link><nav className="navlinks"><Link href="/">Home</Link><Link href="/shop">Shop</Link><Link href="/about">About Us</Link><Link href="/blog">Blog</Link></nav></div></header><section className="content-hero"><div className="container"><div className="eyebrow">Folus Emporium</div><h1>{p.title}</h1>{p.excerpt?<p style={{maxWidth:780}}>{p.excerpt}</p>:null}</div></section><section className="section"><div className="container article-wrap">{String(p.content).split(/\n\n+/).map((x:string,i:number)=><p key={i}>{x}</p>)}</div></section></main>}
